import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles((theme) => ({
    loginImage: {
        marginRight: '10px',
        cursor: 'pointer',
    },
}));

export default useStyles;