import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles((theme) => ({
    tag: {
        borderRadius: 5,
        fontSize: 14,
        color: '#4a4548',
      }
}));

export default useStyles;